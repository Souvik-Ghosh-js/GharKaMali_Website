// Feature flags for the customer website. Flip a flag and the whole UI follows
// (nav links, route guards, homepage sections, etc.).

// Plant Store / shop. Set to false to hide it — the route, nav links,
// homepage preview, and footer links all disappear together.
export const SHOP_ENABLED = true;
