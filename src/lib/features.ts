// Feature flags for the customer website. Flip a flag and the whole UI follows
// (nav links, route guards, homepage sections, etc.).

// Plant Store / shop is disabled for now. Set to true to bring it back —
// the route, nav links, homepage preview, and footer links all re-appear.
export const SHOP_ENABLED = false;
